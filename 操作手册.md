# 多平台便签导出至 Joplin 操作手册

本手册详细介绍了如何从**小米便签、OPPO便签、vivo便签、Apple 备忘录 (iCloud)、Google Keep** 获取便签数据并导出为标准 Markdown，最后导入至 **Joplin** 桌面端。

---

## 目录
1. [环境准备](#一环境准备)
2. [各平台凭据获取步骤](#二各平台凭据获取步骤)
   - [2.1 小米便签 (Xiaomi)](#21-小米便签-xiaomi)
   - [2.2 OPPO 便签 (HeyTap)](#22-oppo-便签-heytap)
   - [2.3 vivo 便签 (vivo Cloud)](#23-vivo-便签-vivo-cloud)
   - [2.4 Apple 备忘录 (iPhone / Mac)](#24-apple-备忘录-iphone--mac)
   - [2.5 Google Keep](#25-google-keep)
3. [运行导出工具](#三运行导出工具)
   - [3.1 交互式菜单模式 (推荐)](#31-交互式菜单模式-推荐)
   - [3.2 命令行直接导出](#32-命令行直接导出)
4. [Joplin 导入步骤](#四joplin-导入步骤)
5. [常见问题排查 (FAQ)](#五常见问题排查-faq)

---

## 一、环境准备

1. **安装 Python 3.8 或更高版本**：
   - 检查命令：`python --version`
2. **（可选）安装第三方依赖**：
   - 工具内置了纯 Python 解析器与 HTTP 客户端，零依赖即可运行基础功能。
   - 若需在线拉取 Google Keep 或使用 iCloud 2FA，可安装扩展依赖：
   ```bash
   pip install -r requirements.txt
   ```

---

## 二、各平台凭据获取步骤

在项目根目录下，工具会自动使用 `config.yaml` 配置文件。

### 2.1 小米便签 (Xiaomi)
1. 在电脑浏览器（Chrome / Edge 等）打开 [小米云服务 (https://i.mi.com)](https://i.mi.com/)。
2. 使用手机小米账号扫码登录。
3. 登录后按键盘 `F12` 打开“开发者工具”，切换到 **网络 (Network)** 面板。
4. 页面左侧点击进入“便签”，在 Network 面板过滤或点击任意一个请求（如 `profile` 或 `page`）。
5. 在右侧 **标头 (Headers) -> 请求标头 (Request Headers)** 中找到 `Cookie:`。
6. 鼠标全选并复制该 Cookie 字符串，打开 `config.yaml` 填入：
   ```yaml
   xiaomi:
     cookie: "serviceToken=xxxx; userId=123456; ..."
   ```

---

### 2.2 OPPO 便签 (HeyTap)
1. 在电脑浏览器打开 [欢太云服务 (https://cloud.heytap.com)](https://cloud.heytap.com/)。
2. 登录你的 OPPO / OnePlus 账号。
3. 按 `F12` 打开开发者工具，切换到 **网络 (Network)**。
4. 点击进入“便签”，在 Network 请求列表中点击任意请求。
5. 复制 **请求标头 (Request Headers)** 中的 `Cookie:` 字段。
6. 填入 `config.yaml`：
   ```yaml
   oppo:
     cookie: "xxxx..."
   ```
   > **注**：若手机已备份导出过 HTML 文件，也可直接填写本地路径：`export_path: "D:/oppo_backup"`。

---

### 2.3 vivo 便签 (vivo Cloud)
1. 在电脑浏览器打开 [vivo 云服务 (https://yun.vivo.com)](https://yun.vivo.com/)。
2. 扫码或账号登录 vivo 账号。
3. 按 `F12` 打开开发者工具 -> **网络 (Network)**。
4. 进入“便签”页面，点击列表中的任意请求，复制 **请求标头** 中的 `Cookie:` 字段。
5. 填入 `config.yaml`：
   ```yaml
   vivo:
     cookie: "xxxx..."
   ```

---

### 2.4 Apple 备忘录 (iPhone / Mac)

#### 方式 A：iCloud 在线拉取（推荐）
1. 在 `config.yaml` 中填入你的 Apple ID 账号与密码：
   ```yaml
   apple:
     apple_id: "your_email@icloud.com"
     password: "your_password"
   ```
2. 运行导出工具时，iPhone / iPad 屏幕会收到 6 位验证码弹窗，直接在终端中输入验证码即可。

#### 方式 B：本地导出文件夹
若开启了“高级数据保护 (ADP)”导致 Web 端不可访问，可在 Mac 备忘录或通过快捷指令导出 HTML/TXT/Markdown 文件，然后配置路径：
```yaml
apple:
  export_path: "D:/apple_notes_export"
```

---

### 2.5 Google Keep

#### 方式 A：Google Takeout 导出（推荐，免密且最稳定）
1. 访问 [Google Takeout (https://takeout.google.com/)](https://takeout.google.com/)。
2. 点击“取消全选”，只勾选 **Keep**。
3. 点击“下一步”并创建导出，下载解压 `.zip` 压缩包。
4. 在 `config.yaml` 中填入解压后的文件夹路径：
   ```yaml
   google:
     takeout_path: "D:/Downloads/Takeout/Keep"
   ```

#### 方式 B：在线账号密码拉取
1. 在 Google 账号管理 -> 安全性中开启两步验证，并生成一个“应用专用密码 (App Password)”。
2. 在 `config.yaml` 中配置：
   ```yaml
   google:
     username: "example@gmail.com"
     password: "app_password_here"
   ```

---

## 三、运行导出工具

### 3.1 交互式菜单模式 (推荐)
在终端运行：
```bash
python main.py
```
终端将显示图形化操作菜单：
```text
=====================================================
    多平台便签导出工具 (Multi-Platform Notes Exporter)
    支持: 小米 | OPPO | vivo | Apple | Google -> Joplin
=====================================================

请选择要导出的平台:
 [1] 小米便签 (Xiaomi Notes)
 [2] OPPO 便签 (HeyTap Cloud)
 [3] vivo 便签 (OriginOS Cloud)
 [4] Apple 备忘录 (iCloud Notes)
 [5] Google Keep
 [6] 全部平台 (批量导出已配置的平台)
 [7] 生成/重置默认 config.yaml 模板
 [0] 退出
```
输入对应数字序号（如 `1` 导出小米便签，或 `6` 导出所有已配置平台）即可自动执行。

---

### 3.2 命令行直接导出
```bash
# 导出指定平台
python main.py -p xiaomi

# 命令行直接传入 Cookie（无需改配置文件）
python main.py -p xiaomi --cookie "serviceToken=xxx; userId=yyy"

# 自定义导出目标目录
python main.py -p all -o "D:/MyNotesExport"
```

---

## 四、Joplin 导入步骤

导出成功后，输出目录（默认 `export_output/`）结构如下：
```text
export_output/
├── xiaomi/
│   ├── _resources/          # 自动下载的图片、录音和附件
│   │   ├── mi_img_01.jpg
│   │   └── voice_01.mp3
│   ├── 工作/                # 对应源便签中的分类/文件夹
│   │   └── 项目周报.md
│   └── 个人/
│       └── 购物清单.md
├── oppo/
│   ├── _resources/
│   └── 读书笔记.md
└── ...
```

### 导入操作：
1. 打开 **Joplin 桌面客户端**。
2. 点击顶部菜单栏：**文件 (File)** -> **导入 (Import)** -> **MD - Markdown (目录)**。
3. 在弹出的文件选择器中，选中导出的平台目录（例如选择 `export_output/xiaomi` 或整个 `export_output`）。
4. 点击“选择文件夹”开始导入。

### 导入后的效果：
- **分类层级**：Joplin 会自动创建与本地子文件夹同名的笔记本（如 `工作`、`个人`）。
- **待办事项**：源便签中的待办框自动呈现为 Joplin 的复选框（`- [ ]` / `- [x]`）。
- **时间与标签**：笔记的原始创建时间、更新时间及标签元数据完整保留。
- **图片与附件**：`_resources/` 目录中的所有图片和录音会自动存入 Joplin 内部数据库，后续移动或删除导出文件夹不会丢失图片。

---

## 五、常见问题排查 (FAQ)

#### Q1: 提示“Cookie 已失效或无效”怎么办？
- **原因**：部分厂商云服务的网页登录 Cookie 有时效（通常为数天至一个月）。
- **解决办法**：重新在浏览器登录一次云服务页面，按 `F12` 重新复制一份最新 Cookie 覆盖到 `config.yaml` 中即可。

#### Q2: 提示“未找到 gkeepapi 库”或“未找到 pyicloud 库”？
- **解决办法**：
  1. 运行 `pip install -r requirements.txt` 安装相关库。
  2. 或者改用免密离线模式：Google Keep 使用 Google Takeout 导出目录；Apple 使用本地导出目录。

#### Q3: 导出的图片在 Joplin 中能正常显示吗？
- **解答**：完全可以。工具已自动将便签中的图片下载至本地 `_resources/` 文件夹，并生成了 Markdown 相对链接。Joplin 在以“目录”方式导入 Markdown 时，会自动扫描并将图片全部收纳进 Joplin 专属资源库中。
